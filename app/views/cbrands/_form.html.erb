<%= form_for(@cbrand) do |f| %>
  <% if @cbrand.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cbrand.errors.count, "error") %> prohibited this cbrand from being saved:</h2>

      <ul>
      <% @cbrand.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
