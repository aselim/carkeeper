<%= form_for @car do |f| %>
  <% if @car.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@car.errors.count, "error") %> prohibited this car from being saved:</h2>

      <ul>
      <% @car.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<p>
    <%= text_field_tag :brand1, params[:brand] %>
    <%= text_field_tag :model1, params[:model] %>
    <%= text_field_tag :year1, params[:year] %>
    <%= text_field_tag :cc1, params[:cc] %>
    <%= text_field_tag :km1, params[:km] %>
    <%= text_field_tag :price1, params[:price] %>
<%= submit_tag "Search", :sort => "datein" %>
  </p>
<ul class="digg_pagination" align=center><%= link_to 'Seach', cars_path(:sort=>"datein" , :brand=>@car) %></ul>

<% end %>
